set(XCSOARSUBST ${YCSoar_SOURCE_DIR}/src/XCSoarSubst)
set(XCSOARTHUNK ${YCSoar_SOURCE_DIR}/src/XCSoarThunk)
set(EXPERIMENT_SRC ${YCSoar_SOURCE_DIR}/src/Experiment)
include_directories(${XCSOARSUBST}
                    ${XCSOARTHUNK}
                    ${XCSOAR_SRC}
                    ${XCSOAR_SRC}/unix
                    ${XCSOAR_SRC}/Engine/Task
                    ${XCSOAR_SRC}/Engine)

# Disable this warning as an error - necessary for source generated by 'rcc'.
add_compile_options("-Wno-error=missing-declarations")

set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTORCC ON)
set(CMAKE_INCLUDE_CURRENT_DIR ON)

find_package(Qt5Widgets REQUIRED)
find_package(Qt5Qml REQUIRED)
find_package(Qt5 COMPONENTS Quick Core Network)

add_subdirectory(Screen)
add_subdirectory(ModelView)

